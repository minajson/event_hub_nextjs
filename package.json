{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/eventsphere",
    "artifacts/api-server",
    "lib/api-client-react",
    "lib/api-zod",
    "lib/db",
    "scripts"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=artifacts/eventsphere",
    "build": "npm run typecheck && npm run build --workspace=artifacts/eventsphere --workspace=artifacts/api-server --if-present",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run typecheck --workspace=artifacts/eventsphere --workspace=artifacts/api-server --workspace=scripts --if-present"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
